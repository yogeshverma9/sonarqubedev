apiVersion: v1
kind: Service
metadata:
  name: {{ include "sonarqube-enterprise.fullname" . }}
  labels:
    {{- include "sonarqube-enterprise.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "sonarqube-enterprise.selectorLabels" . | nindent 4 }}
